{
  "name": "weaverfi-api",
  "description": "The API that powers WeaverFi.",
  "version": "0.3.2",
  "scripts": {
    "build": "cd functions && npm run build",
    "start": "npm run build && node functions/lib/src/index.js"
  },
  "dependencies": {}
}
