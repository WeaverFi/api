{
  "name": "weaverfi-api",
  "description": "The API that powers WeaverFi.",
  "version": "1.10.3",
  "scripts": {
    "build": "cd functions && npm run build",
    "start": "npm run build && node functions/lib/index.js"
  },
  "dependencies": {}
}